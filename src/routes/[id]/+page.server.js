export async function load ({ url, params }) {
    //hier komt het fetchen van data check oude server.js met alles

    // return {member: membersData.data} // Return hier dus de data (Dit is geen render)
}